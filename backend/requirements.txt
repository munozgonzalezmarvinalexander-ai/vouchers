# Dependencias para correr la API y los scripts.
SQLAlchemy>=2.0
psycopg2-binary>=2.9      # driver de PostgreSQL (Neon). Trae binarios: no compila nada.
python-dotenv>=1.0        # lee el archivo .env
openpyxl>=3.1            # leer el Excel en el script de extracción
fastapi>=0.110           # framework de la API
uvicorn[standard]>=0.29  # servidor para correr la API
jinja2>=3.1               # plantilla del voucher (HTML/PDF)
pyspellchecker>=0.8        # corrección ortográfica en español
